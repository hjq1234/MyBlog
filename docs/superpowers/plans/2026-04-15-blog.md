# Django 个人博客 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建本地个人博客网站，支持富文本写作（含图片上传）、标签分类、全文搜索和明暗模式切换。

**Architecture:** 单 Django App `blog`，Django Admin + django-summernote 作为写作后台，Bootstrap 5 构建前台阅读界面，SQLite 存储数据，Bootstrap 5 的 `data-bs-theme` 属性配合 localStorage 实现明暗切换。

**Tech Stack:** Django 4.x, django-summernote, Pillow, Bootstrap 5 (CDN), SQLite

---

## File Map

| 文件 | 职责 |
|---|---|
| `requirements.txt` | Python 依赖声明 |
| `myblog/settings.py` | Django 配置（由 startproject 生成，需修改） |
| `myblog/urls.py` | 根路由（由 startproject 生成，需修改） |
| `blog/models.py` | Tag、Post 数据模型 |
| `blog/admin.py` | Django Admin 注册，集成 Summernote |
| `blog/context_processors.py` | 标签云上下文处理器，向所有模板注入 `all_tags` |
| `blog/views.py` | 所有前台视图（IndexView、PostListView、PostDetailView、TagPostsView、SearchView） |
| `blog/urls.py` | blog app 路由 |
| `blog/tests.py` | 所有单元测试 |
| `blog/templates/blog/base.html` | 公共布局（导航、侧边栏、明暗切换按钮） |
| `blog/templates/blog/index.html` | 首页：最近文章列表 |
| `blog/templates/blog/post_list.html` | 文章列表页：分页 |
| `blog/templates/blog/post_detail.html` | 文章详情页：全文 + 上下篇导航 |
| `blog/templates/blog/tag_posts.html` | 标签筛选页 |
| `blog/templates/blog/search.html` | 搜索结果页 |
| `blog/static/blog/css/custom.css` | 明暗模式 CSS 变量、标签徽章样式 |
| `blog/static/blog/js/theme.js` | 主题切换逻辑（IIFE + toggleTheme 函数） |

---

## Task 1: Project Bootstrap

**Files:**
- Create: `requirements.txt`
- Create: `myblog/` (via django-admin startproject)
- Create: `blog/` (via manage.py startapp)
- Modify: `myblog/settings.py`
- Modify: `myblog/urls.py`
- Create: `blog/urls.py`

- [ ] **Step 1: 创建 requirements.txt**

```
Django>=4.2,<6.0
django-summernote>=0.8.20
Pillow>=10.0
```

- [ ] **Step 2: 安装依赖**

```bash
.venv/Scripts/python -m pip install -r requirements.txt
```

期望输出：Successfully installed Django-... django-summernote-... Pillow-...

- [ ] **Step 3: 创建 Django 项目**

```bash
.venv/Scripts/python -m django startproject myblog .
```

在当前目录生成 `myblog/` 和 `manage.py`。

- [ ] **Step 4: 创建 blog app**

```bash
.venv/Scripts/python manage.py startapp blog
```

生成 `blog/` 目录。

- [ ] **Step 5: 修改 myblog/settings.py**

找到 `INSTALLED_APPS`，替换为：

```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'django_summernote',
    'blog',
]
```

找到 `TEMPLATES` 列表中的 `'OPTIONS'` → `'context_processors'`，添加一行：

```python
'blog.context_processors.tag_cloud',
```

使得完整的 context_processors 列表如下：

```python
'context_processors': [
    'django.template.context_processors.debug',
    'django.template.context_processors.request',
    'django.contrib.auth.context_processors.auth',
    'django.contrib.messages.context_processors.messages',
    'blog.context_processors.tag_cloud',
],
```

在文件末尾追加：

```python
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

X_FRAME_OPTIONS = 'SAMEORIGIN'

SUMMERNOTE_CONFIG = {
    'summernote': {
        'width': '100%',
        'height': '480',
    },
}
```

- [ ] **Step 6: 修改 myblog/urls.py**

将文件内容替换为：

```python
from django.contrib import admin
from django.urls import path, include
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    path('admin/', admin.site.urls),
    path('summernote/', include('django_summernote.urls')),
    path('', include('blog.urls')),
] + static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
```

- [ ] **Step 7: 创建 blog/urls.py（空桩）**

```python
from django.urls import path
from . import views

urlpatterns = []
```

- [ ] **Step 8: 验证项目可启动**

```bash
.venv/Scripts/python manage.py check
```

期望输出：`System check identified no issues (0 silenced).`

- [ ] **Step 9: Commit**

```bash
git add requirements.txt myblog/ blog/ manage.py
git commit -m "feat: bootstrap Django project with blog app"
```

---

## Task 2: Data Models

**Files:**
- Modify: `blog/models.py`
- Modify: `blog/tests.py`

- [ ] **Step 1: 写失败测试，替换 blog/tests.py 内容为：**

```python
from django.test import TestCase
from .models import Tag, Post


class TagModelTest(TestCase):
    def test_slug_auto_generated_from_name(self):
        tag = Tag.objects.create(name='Django Tips')
        self.assertEqual(tag.slug, 'django-tips')

    def test_str_returns_name(self):
        tag = Tag.objects.create(name='Python', slug='python')
        self.assertEqual(str(tag), 'Python')


class PostModelTest(TestCase):
    def setUp(self):
        self.post = Post.objects.create(
            title='Test Post',
            content='<p>Hello <b>world</b></p>',
            is_draft=False,
        )

    def test_get_summary_strips_html_when_no_summary_field(self):
        self.assertEqual(self.post.get_summary(), 'Hello world')

    def test_get_summary_uses_summary_field_when_set(self):
        self.post.summary = 'My custom summary'
        self.post.save()
        self.assertEqual(self.post.get_summary(), 'My custom summary')

    def test_default_ordering_newest_first(self):
        from django.utils import timezone
        from datetime import timedelta
        Post.objects.filter(pk=self.post.pk).update(
            created_at=timezone.now() - timedelta(days=1)
        )
        post2 = Post.objects.create(title='Newer', content='x', is_draft=False)
        posts = list(Post.objects.all())
        self.assertEqual(posts[0], post2)

    def test_is_draft_default_is_true(self):
        post = Post.objects.create(title='No Draft Flag', content='x')
        self.assertTrue(post.is_draft)
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv/Scripts/python manage.py test blog.tests.TagModelTest blog.tests.PostModelTest -v 2
```

期望：`ImportError` 或 `cannot import name 'Tag'`（模型尚未实现）。

- [ ] **Step 3: 实现 blog/models.py**

```python
import re
from django.db import models
from django.utils.text import slugify
from django_summernote.fields import SummernoteTextField


class Tag(models.Model):
    name = models.CharField(max_length=50, unique=True)
    slug = models.SlugField(max_length=50, unique=True)

    def save(self, *args, **kwargs):
        if not self.slug:
            self.slug = slugify(self.name, allow_unicode=True)
        super().save(*args, **kwargs)

    def __str__(self):
        return self.name


class Post(models.Model):
    title = models.CharField(max_length=200)
    content = SummernoteTextField()
    summary = models.TextField(blank=True)
    cover_image = models.ImageField(upload_to='covers/', blank=True)
    tags = models.ManyToManyField(Tag, blank=True, related_name='posts')
    is_draft = models.BooleanField(default=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        ordering = ['-created_at']

    def get_summary(self):
        if self.summary:
            return self.summary
        clean = re.sub(r'<[^>]+>', '', self.content)
        return clean[:150].strip()

    def get_absolute_url(self):
        from django.urls import reverse
        return reverse('post_detail', args=[self.pk])

    def __str__(self):
        return self.title
```

- [ ] **Step 4: 生成并应用迁移**

```bash
.venv/Scripts/python manage.py makemigrations blog
.venv/Scripts/python manage.py migrate
```

期望：`Applying blog.0001_initial... OK`

- [ ] **Step 5: 运行测试，确认通过**

```bash
.venv/Scripts/python manage.py test blog.tests.TagModelTest blog.tests.PostModelTest -v 2
```

期望：`Ran 5 tests in ...s  OK`

- [ ] **Step 6: Commit**

```bash
git add blog/models.py blog/tests.py blog/migrations/
git commit -m "feat: add Tag and Post models with tests"
```

---

## Task 3: Admin Configuration

**Files:**
- Modify: `blog/admin.py`
- Modify: `blog/tests.py`

- [ ] **Step 1: 在 blog/tests.py 末尾追加测试类**

```python
class AdminRegistrationTest(TestCase):
    def test_post_registered_in_admin(self):
        from django.contrib import admin
        from .models import Post
        self.assertIn(Post, admin.site._registry)

    def test_tag_registered_in_admin(self):
        from django.contrib import admin
        from .models import Tag
        self.assertIn(Tag, admin.site._registry)
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv/Scripts/python manage.py test blog.tests.AdminRegistrationTest -v 2
```

期望：`FAIL` — Post/Tag 未注册。

- [ ] **Step 3: 实现 blog/admin.py**

```python
from django.contrib import admin
from django_summernote.admin import SummernoteModelAdmin
from .models import Tag, Post


@admin.register(Tag)
class TagAdmin(admin.ModelAdmin):
    prepopulated_fields = {'slug': ('name',)}
    list_display = ('name', 'slug')


@admin.register(Post)
class PostAdmin(SummernoteModelAdmin):
    summernote_fields = ('content',)
    list_display = ('title', 'is_draft', 'created_at', 'updated_at')
    list_filter = ('is_draft', 'tags')
    search_fields = ('title',)
    filter_horizontal = ('tags',)
    list_editable = ('is_draft',)
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
.venv/Scripts/python manage.py test blog.tests.AdminRegistrationTest -v 2
```

期望：`Ran 2 tests in ...s  OK`

- [ ] **Step 5: 创建超级用户（交互式，手动执行）**

```bash
.venv/Scripts/python manage.py createsuperuser
```

输入用户名、邮箱（可留空）、密码。

- [ ] **Step 6: Commit**

```bash
git add blog/admin.py blog/tests.py
git commit -m "feat: configure Django admin with Summernote editor"
```

---

## Task 4: Tag Cloud Context Processor

**Files:**
- Create: `blog/context_processors.py`
- Modify: `blog/tests.py`

- [ ] **Step 1: 在 blog/tests.py 末尾追加测试类**

```python
class TagCloudContextProcessorTest(TestCase):
    def test_returns_all_tags(self):
        from django.test import RequestFactory
        from .context_processors import tag_cloud
        from .models import Tag
        Tag.objects.create(name='Python', slug='python')
        Tag.objects.create(name='Django', slug='django')
        factory = RequestFactory()
        request = factory.get('/')
        result = tag_cloud(request)
        self.assertIn('all_tags', result)
        self.assertEqual(result['all_tags'].count(), 2)
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv/Scripts/python manage.py test blog.tests.TagCloudContextProcessorTest -v 2
```

期望：`ImportError: cannot import name 'tag_cloud'`

- [ ] **Step 3: 创建 blog/context_processors.py**

```python
from .models import Tag


def tag_cloud(request):
    return {'all_tags': Tag.objects.all()}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
.venv/Scripts/python manage.py test blog.tests.TagCloudContextProcessorTest -v 2
```

期望：`Ran 1 test in ...s  OK`

- [ ] **Step 5: Commit**

```bash
git add blog/context_processors.py blog/tests.py
git commit -m "feat: add tag cloud context processor"
```

---

## Task 5: Static Files & Base Template

**Files:**
- Create: `blog/static/blog/css/custom.css`
- Create: `blog/static/blog/js/theme.js`
- Create: `blog/templates/blog/base.html`

（本任务为模板/静态资源，无单元测试）

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p blog/static/blog/css
mkdir -p blog/static/blog/js
mkdir -p blog/templates/blog
```

- [ ] **Step 2: 创建 blog/static/blog/js/theme.js**

```javascript
(function () {
    const saved = localStorage.getItem('theme') || 'light';
    document.documentElement.setAttribute('data-bs-theme', saved);
    document.addEventListener('DOMContentLoaded', function () {
        const icon = document.getElementById('theme-icon');
        if (icon) {
            icon.textContent = saved === 'dark' ? '☀️' : '🌙';
        }
    });
})();

function toggleTheme() {
    const html = document.documentElement;
    const next = html.getAttribute('data-bs-theme') === 'light' ? 'dark' : 'light';
    html.setAttribute('data-bs-theme', next);
    localStorage.setItem('theme', next);
    document.getElementById('theme-icon').textContent = next === 'dark' ? '☀️' : '🌙';
}
```

- [ ] **Step 3: 创建 blog/static/blog/css/custom.css**

```css
:root[data-bs-theme="light"] {
    --tag-bg: #e8f0fe;
    --tag-color: #1a73e8;
}

:root[data-bs-theme="dark"] {
    --tag-bg: #1e3a5f;
    --tag-color: #7ab3f0;
}

.tag-badge {
    background-color: var(--tag-bg);
    color: var(--tag-color);
    padding: 2px 10px;
    border-radius: 12px;
    font-size: 0.78rem;
    text-decoration: none;
    display: inline-block;
    margin: 2px 2px 2px 0;
    transition: opacity 0.15s;
}

.tag-badge:hover {
    opacity: 0.8;
    color: var(--tag-color);
}

.post-card {
    border: none;
    box-shadow: 0 1px 4px rgba(0, 0, 0, 0.08);
    transition: box-shadow 0.15s;
}

.post-card:hover {
    box-shadow: 0 3px 10px rgba(0, 0, 0, 0.12);
}
```

- [ ] **Step 4: 创建 blog/templates/blog/base.html**

```html
<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}我的博客{% endblock %}</title>
    {% load static %}
    <script src="{% static 'blog/js/theme.js' %}"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="{% static 'blog/css/custom.css' %}">
</head>
<body>
<nav class="navbar navbar-expand-lg border-bottom mb-4">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{% url 'index' %}">📝 我的博客</a>
        <div class="d-flex align-items-center gap-3 ms-auto">
            <a href="{% url 'index' %}" class="nav-link">首页</a>
            <a href="{% url 'post_list' %}" class="nav-link">文章</a>
            <form class="d-flex" action="{% url 'search' %}" method="get">
                <input class="form-control form-control-sm me-2" type="search" name="q"
                       placeholder="搜索..." value="{{ request.GET.q|default:'' }}" style="width:140px;">
                <button class="btn btn-sm btn-outline-secondary" type="submit">搜</button>
            </form>
            <button class="btn btn-sm border-0" onclick="toggleTheme()" title="切换主题">
                <span id="theme-icon">🌙</span>
            </button>
        </div>
    </div>
</nav>

<div class="container mb-5">
    <div class="row g-4">
        <div class="col-lg-8">
            {% block content %}{% endblock %}
        </div>
        <div class="col-lg-4">
            {% block sidebar %}
            <div class="card border-0 shadow-sm">
                <div class="card-body">
                    <h6 class="card-title mb-3">🏷️ 标签</h6>
                    {% for tag in all_tags %}
                    <a href="{% url 'tag_posts' tag.slug %}" class="tag-badge">{{ tag.name }}</a>
                    {% empty %}
                    <p class="text-muted small mb-0">暂无标签</p>
                    {% endfor %}
                </div>
            </div>
            {% endblock %}
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
```

- [ ] **Step 5: Commit**

```bash
git add blog/static/ blog/templates/
git commit -m "feat: add base template, theme toggle, and custom CSS"
```

---

## Task 6: Index View

**Files:**
- Modify: `blog/views.py`
- Modify: `blog/urls.py`
- Create: `blog/templates/blog/index.html`
- Modify: `blog/tests.py`

- [ ] **Step 1: 在 blog/tests.py 末尾追加测试类**

```python
class IndexViewTest(TestCase):
    def setUp(self):
        from .models import Post
        Post.objects.create(title='Published Post', content='<p>Hello</p>', is_draft=False)
        Post.objects.create(title='Draft Post', content='<p>Hidden</p>', is_draft=True)

    def test_returns_200(self):
        response = self.client.get('/')
        self.assertEqual(response.status_code, 200)

    def test_uses_index_template(self):
        response = self.client.get('/')
        self.assertTemplateUsed(response, 'blog/index.html')

    def test_shows_only_published_posts(self):
        response = self.client.get('/')
        titles = [p.title for p in response.context['posts']]
        self.assertIn('Published Post', titles)
        self.assertNotIn('Draft Post', titles)

    def test_all_tags_in_context(self):
        from .models import Tag
        Tag.objects.create(name='Python', slug='python')
        response = self.client.get('/')
        self.assertIn('all_tags', response.context)
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv/Scripts/python manage.py test blog.tests.IndexViewTest -v 2
```

期望：`404` 或 `NoReverseMatch`（视图尚未实现）。

- [ ] **Step 3: 实现 blog/views.py**

```python
from django.shortcuts import get_object_or_404
from django.views.generic import ListView, DetailView
from django.db.models import Q
from .models import Post, Tag


class IndexView(ListView):
    template_name = 'blog/index.html'
    context_object_name = 'posts'
    paginate_by = 10

    def get_queryset(self):
        return Post.objects.filter(is_draft=False)


class PostListView(ListView):
    template_name = 'blog/post_list.html'
    context_object_name = 'posts'
    paginate_by = 10

    def get_queryset(self):
        return Post.objects.filter(is_draft=False)


class PostDetailView(DetailView):
    template_name = 'blog/post_detail.html'
    context_object_name = 'post'

    def get_queryset(self):
        return Post.objects.filter(is_draft=False)

    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        post = self.object
        context['prev_post'] = (
            Post.objects.filter(is_draft=False, created_at__lt=post.created_at)
            .order_by('-created_at')
            .first()
        )
        context['next_post'] = (
            Post.objects.filter(is_draft=False, created_at__gt=post.created_at)
            .order_by('created_at')
            .first()
        )
        return context


class TagPostsView(ListView):
    template_name = 'blog/tag_posts.html'
    context_object_name = 'posts'
    paginate_by = 10

    def get_queryset(self):
        self.tag = get_object_or_404(Tag, slug=self.kwargs['slug'])
        return Post.objects.filter(tags=self.tag, is_draft=False)

    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['tag'] = self.tag
        return context


class SearchView(ListView):
    template_name = 'blog/search.html'
    context_object_name = 'posts'
    paginate_by = 10

    def get_queryset(self):
        self.query = self.request.GET.get('q', '').strip()
        if not self.query:
            return Post.objects.none()
        return Post.objects.filter(
            Q(title__icontains=self.query) | Q(content__icontains=self.query),
            is_draft=False,
        ).distinct()

    def get_context_data(self, **kwargs):
        context = super().get_context_data(**kwargs)
        context['query'] = self.query
        return context
```

- [ ] **Step 4: 将 blog/urls.py 内容替换为：**

```python
from django.urls import path
from . import views

urlpatterns = [
    path('', views.IndexView.as_view(), name='index'),
    path('posts/', views.PostListView.as_view(), name='post_list'),
    path('posts/<int:pk>/', views.PostDetailView.as_view(), name='post_detail'),
    path('tag/<slug:slug>/', views.TagPostsView.as_view(), name='tag_posts'),
    path('search/', views.SearchView.as_view(), name='search'),
]
```

- [ ] **Step 5: 创建 blog/templates/blog/index.html**

```html
{% extends 'blog/base.html' %}

{% block title %}首页 - 我的博客{% endblock %}

{% block content %}
<h5 class="mb-4 fw-normal text-muted">最近文章</h5>

{% for post in posts %}
<div class="card post-card mb-3">
    {% if post.cover_image %}
    <img src="{{ post.cover_image.url }}" class="card-img-top"
         style="height:200px;object-fit:cover;" alt="{{ post.title }}">
    {% endif %}
    <div class="card-body">
        <h5 class="card-title mb-1">
            <a href="{{ post.get_absolute_url }}" class="text-decoration-none">{{ post.title }}</a>
        </h5>
        <p class="card-text text-muted small my-2">{{ post.get_summary }}</p>
        <div class="d-flex justify-content-between align-items-center">
            <div>
                {% for tag in post.tags.all %}
                <a href="{% url 'tag_posts' tag.slug %}" class="tag-badge">{{ tag.name }}</a>
                {% endfor %}
            </div>
            <small class="text-muted">{{ post.created_at|date:"Y-m-d" }}</small>
        </div>
    </div>
</div>
{% empty %}
<div class="text-center py-5">
    <p class="text-muted">还没有文章，去 <a href="/admin/">后台</a> 写第一篇吧！</p>
</div>
{% endfor %}

{% if is_paginated %}
<nav class="mt-4">
    <ul class="pagination justify-content-center">
        {% if page_obj.has_previous %}
        <li class="page-item">
            <a class="page-link" href="?page={{ page_obj.previous_page_number }}">上一页</a>
        </li>
        {% endif %}
        <li class="page-item disabled">
            <span class="page-link">{{ page_obj.number }} / {{ page_obj.paginator.num_pages }}</span>
        </li>
        {% if page_obj.has_next %}
        <li class="page-item">
            <a class="page-link" href="?page={{ page_obj.next_page_number }}">下一页</a>
        </li>
        {% endif %}
    </ul>
</nav>
{% endif %}
{% endblock %}
```

- [ ] **Step 6: 运行测试，确认通过**

```bash
.venv/Scripts/python manage.py test blog.tests.IndexViewTest -v 2
```

期望：`Ran 4 tests in ...s  OK`

- [ ] **Step 7: Commit**

```bash
git add blog/views.py blog/urls.py blog/templates/blog/index.html blog/tests.py
git commit -m "feat: add index view with published post list"
```

---

## Task 7: Post List View

**Files:**
- Create: `blog/templates/blog/post_list.html`
- Modify: `blog/tests.py`

（PostListView 已在 Task 6 的 views.py 中实现）

- [ ] **Step 1: 在 blog/tests.py 末尾追加测试类**

```python
class PostListViewTest(TestCase):
    def test_returns_200(self):
        response = self.client.get('/posts/')
        self.assertEqual(response.status_code, 200)

    def test_uses_post_list_template(self):
        response = self.client.get('/posts/')
        self.assertTemplateUsed(response, 'blog/post_list.html')

    def test_pagination_shows_ten_per_page(self):
        from .models import Post
        for i in range(12):
            Post.objects.create(title=f'Post {i}', content='x', is_draft=False)
        response = self.client.get('/posts/')
        self.assertEqual(len(response.context['posts']), 10)
        response2 = self.client.get('/posts/?page=2')
        self.assertEqual(len(response2.context['posts']), 2)

    def test_drafts_excluded(self):
        from .models import Post
        Post.objects.create(title='Visible', content='x', is_draft=False)
        Post.objects.create(title='Hidden', content='x', is_draft=True)
        response = self.client.get('/posts/')
        titles = [p.title for p in response.context['posts']]
        self.assertNotIn('Hidden', titles)
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv/Scripts/python manage.py test blog.tests.PostListViewTest -v 2
```

期望：`TemplateDoesNotExist: blog/post_list.html`

- [ ] **Step 3: 创建 blog/templates/blog/post_list.html**

```html
{% extends 'blog/base.html' %}

{% block title %}文章列表 - 我的博客{% endblock %}

{% block content %}
<h5 class="mb-4 fw-normal text-muted">全部文章</h5>

{% for post in posts %}
<div class="card post-card mb-3">
    {% if post.cover_image %}
    <img src="{{ post.cover_image.url }}" class="card-img-top"
         style="height:200px;object-fit:cover;" alt="{{ post.title }}">
    {% endif %}
    <div class="card-body">
        <h5 class="card-title mb-1">
            <a href="{{ post.get_absolute_url }}" class="text-decoration-none">{{ post.title }}</a>
        </h5>
        <p class="card-text text-muted small my-2">{{ post.get_summary }}</p>
        <div class="d-flex justify-content-between align-items-center">
            <div>
                {% for tag in post.tags.all %}
                <a href="{% url 'tag_posts' tag.slug %}" class="tag-badge">{{ tag.name }}</a>
                {% endfor %}
            </div>
            <small class="text-muted">{{ post.created_at|date:"Y-m-d" }}</small>
        </div>
    </div>
</div>
{% empty %}
<p class="text-muted text-center py-4">暂无文章。</p>
{% endfor %}

{% if is_paginated %}
<nav class="mt-4">
    <ul class="pagination justify-content-center">
        {% if page_obj.has_previous %}
        <li class="page-item">
            <a class="page-link" href="?page={{ page_obj.previous_page_number }}">上一页</a>
        </li>
        {% endif %}
        <li class="page-item disabled">
            <span class="page-link">{{ page_obj.number }} / {{ page_obj.paginator.num_pages }}</span>
        </li>
        {% if page_obj.has_next %}
        <li class="page-item">
            <a class="page-link" href="?page={{ page_obj.next_page_number }}">下一页</a>
        </li>
        {% endif %}
    </ul>
</nav>
{% endif %}
{% endblock %}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
.venv/Scripts/python manage.py test blog.tests.PostListViewTest -v 2
```

期望：`Ran 4 tests in ...s  OK`

- [ ] **Step 5: Commit**

```bash
git add blog/templates/blog/post_list.html blog/tests.py
git commit -m "feat: add post list view with pagination"
```

---

## Task 8: Post Detail View

**Files:**
- Create: `blog/templates/blog/post_detail.html`
- Modify: `blog/tests.py`

（PostDetailView 已在 Task 6 的 views.py 中实现）

- [ ] **Step 1: 在 blog/tests.py 末尾追加测试类**

```python
class PostDetailViewTest(TestCase):
    def setUp(self):
        from .models import Post
        from django.utils import timezone
        from datetime import timedelta
        self.post1 = Post.objects.create(title='First Post', content='<p>First</p>', is_draft=False)
        Post.objects.filter(pk=self.post1.pk).update(
            created_at=timezone.now() - timedelta(days=1)
        )
        self.post2 = Post.objects.create(title='Second Post', content='<p>Second</p>', is_draft=False)

    def test_returns_200_for_published_post(self):
        response = self.client.get(f'/posts/{self.post2.pk}/')
        self.assertEqual(response.status_code, 200)

    def test_returns_404_for_draft(self):
        from .models import Post
        draft = Post.objects.create(title='Draft', content='x', is_draft=True)
        response = self.client.get(f'/posts/{draft.pk}/')
        self.assertEqual(response.status_code, 404)

    def test_uses_post_detail_template(self):
        response = self.client.get(f'/posts/{self.post2.pk}/')
        self.assertTemplateUsed(response, 'blog/post_detail.html')

    def test_prev_post_in_context(self):
        self.post1.refresh_from_db()
        response = self.client.get(f'/posts/{self.post2.pk}/')
        self.assertEqual(response.context['prev_post'], self.post1)

    def test_next_post_is_none_for_latest(self):
        response = self.client.get(f'/posts/{self.post2.pk}/')
        self.assertIsNone(response.context['next_post'])

    def test_get_absolute_url(self):
        from django.urls import reverse
        self.assertEqual(
            self.post1.get_absolute_url(),
            reverse('post_detail', args=[self.post1.pk])
        )
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv/Scripts/python manage.py test blog.tests.PostDetailViewTest -v 2
```

期望：`TemplateDoesNotExist: blog/post_detail.html`

- [ ] **Step 3: 创建 blog/templates/blog/post_detail.html**

```html
{% extends 'blog/base.html' %}

{% block title %}{{ post.title }} - 我的博客{% endblock %}

{% block content %}
<article>
    {% if post.cover_image %}
    <img src="{{ post.cover_image.url }}" class="img-fluid rounded mb-4"
         style="max-height:360px;width:100%;object-fit:cover;" alt="{{ post.title }}">
    {% endif %}

    <h1 class="h3 mb-2">{{ post.title }}</h1>

    <div class="d-flex align-items-center gap-3 mb-3 text-muted small">
        <span>📅 {{ post.created_at|date:"Y年m月d日" }}</span>
        {% if post.updated_at != post.created_at %}
        <span>✏️ 更新于 {{ post.updated_at|date:"Y-m-d" }}</span>
        {% endif %}
    </div>

    {% if post.tags.all %}
    <div class="mb-4">
        {% for tag in post.tags.all %}
        <a href="{% url 'tag_posts' tag.slug %}" class="tag-badge">{{ tag.name }}</a>
        {% endfor %}
    </div>
    {% endif %}

    <hr class="mb-4">

    <div class="post-content lh-lg">
        {{ post.content|safe }}
    </div>

    <hr class="mt-5 mb-4">

    <div class="d-flex justify-content-between">
        {% if prev_post %}
        <a href="{{ prev_post.get_absolute_url }}" class="btn btn-sm btn-outline-secondary">
            ← {{ prev_post.title }}
        </a>
        {% else %}
        <span></span>
        {% endif %}
        {% if next_post %}
        <a href="{{ next_post.get_absolute_url }}" class="btn btn-sm btn-outline-secondary">
            {{ next_post.title }} →
        </a>
        {% endif %}
    </div>
</article>
{% endblock %}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
.venv/Scripts/python manage.py test blog.tests.PostDetailViewTest -v 2
```

期望：`Ran 6 tests in ...s  OK`

- [ ] **Step 5: Commit**

```bash
git add blog/templates/blog/post_detail.html blog/tests.py
git commit -m "feat: add post detail view with prev/next navigation"
```

---

## Task 9: Tag Posts View

**Files:**
- Create: `blog/templates/blog/tag_posts.html`
- Modify: `blog/tests.py`

（TagPostsView 已在 Task 6 的 views.py 中实现）

- [ ] **Step 1: 在 blog/tests.py 末尾追加测试类**

```python
class TagPostsViewTest(TestCase):
    def setUp(self):
        from .models import Tag, Post
        self.tag = Tag.objects.create(name='Python', slug='python')
        post = Post.objects.create(title='Tagged Post', content='x', is_draft=False)
        post.tags.add(self.tag)

    def test_returns_200(self):
        response = self.client.get('/tag/python/')
        self.assertEqual(response.status_code, 200)

    def test_returns_404_for_nonexistent_tag(self):
        response = self.client.get('/tag/nonexistent/')
        self.assertEqual(response.status_code, 404)

    def test_uses_tag_posts_template(self):
        response = self.client.get('/tag/python/')
        self.assertTemplateUsed(response, 'blog/tag_posts.html')

    def test_shows_only_posts_with_this_tag(self):
        from .models import Post
        Post.objects.create(title='Untagged', content='x', is_draft=False)
        response = self.client.get('/tag/python/')
        titles = [p.title for p in response.context['posts']]
        self.assertIn('Tagged Post', titles)
        self.assertNotIn('Untagged', titles)

    def test_tag_in_context(self):
        response = self.client.get('/tag/python/')
        self.assertEqual(response.context['tag'], self.tag)
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv/Scripts/python manage.py test blog.tests.TagPostsViewTest -v 2
```

期望：`TemplateDoesNotExist: blog/tag_posts.html`

- [ ] **Step 3: 创建 blog/templates/blog/tag_posts.html**

```html
{% extends 'blog/base.html' %}

{% block title %}标签：{{ tag.name }} - 我的博客{% endblock %}

{% block content %}
<h5 class="mb-4 fw-normal text-muted">
    🏷️ 标签：<strong>{{ tag.name }}</strong>
    <small class="ms-2 fs-6">（{{ paginator.count }} 篇）</small>
</h5>

{% for post in posts %}
<div class="card post-card mb-3">
    {% if post.cover_image %}
    <img src="{{ post.cover_image.url }}" class="card-img-top"
         style="height:200px;object-fit:cover;" alt="{{ post.title }}">
    {% endif %}
    <div class="card-body">
        <h5 class="card-title mb-1">
            <a href="{{ post.get_absolute_url }}" class="text-decoration-none">{{ post.title }}</a>
        </h5>
        <p class="card-text text-muted small my-2">{{ post.get_summary }}</p>
        <div class="d-flex justify-content-between align-items-center">
            <div>
                {% for t in post.tags.all %}
                <a href="{% url 'tag_posts' t.slug %}" class="tag-badge">{{ t.name }}</a>
                {% endfor %}
            </div>
            <small class="text-muted">{{ post.created_at|date:"Y-m-d" }}</small>
        </div>
    </div>
</div>
{% empty %}
<p class="text-muted text-center py-4">该标签下暂无文章。</p>
{% endfor %}

{% if is_paginated %}
<nav class="mt-4">
    <ul class="pagination justify-content-center">
        {% if page_obj.has_previous %}
        <li class="page-item">
            <a class="page-link" href="?page={{ page_obj.previous_page_number }}">上一页</a>
        </li>
        {% endif %}
        <li class="page-item disabled">
            <span class="page-link">{{ page_obj.number }} / {{ page_obj.paginator.num_pages }}</span>
        </li>
        {% if page_obj.has_next %}
        <li class="page-item">
            <a class="page-link" href="?page={{ page_obj.next_page_number }}">下一页</a>
        </li>
        {% endif %}
    </ul>
</nav>
{% endif %}
{% endblock %}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
.venv/Scripts/python manage.py test blog.tests.TagPostsViewTest -v 2
```

期望：`Ran 5 tests in ...s  OK`

- [ ] **Step 5: Commit**

```bash
git add blog/templates/blog/tag_posts.html blog/tests.py
git commit -m "feat: add tag filter view"
```

---

## Task 10: Search View

**Files:**
- Create: `blog/templates/blog/search.html`
- Modify: `blog/tests.py`

（SearchView 已在 Task 6 的 views.py 中实现）

- [ ] **Step 1: 在 blog/tests.py 末尾追加测试类**

```python
class SearchViewTest(TestCase):
    def setUp(self):
        from .models import Post
        Post.objects.create(title='Django Tutorial', content='<p>Learn Django</p>', is_draft=False)
        Post.objects.create(title='Python Basics', content='<p>Learn Python here</p>', is_draft=False)
        Post.objects.create(title='Secret Draft', content='<p>hidden content</p>', is_draft=True)

    def test_returns_200(self):
        response = self.client.get('/search/?q=django')
        self.assertEqual(response.status_code, 200)

    def test_uses_search_template(self):
        response = self.client.get('/search/?q=django')
        self.assertTemplateUsed(response, 'blog/search.html')

    def test_search_by_title(self):
        response = self.client.get('/search/?q=Django')
        self.assertEqual(len(response.context['posts']), 1)
        self.assertEqual(response.context['posts'][0].title, 'Django Tutorial')

    def test_search_by_content(self):
        response = self.client.get('/search/?q=Python+here')
        self.assertEqual(len(response.context['posts']), 1)
        self.assertEqual(response.context['posts'][0].title, 'Python Basics')

    def test_drafts_excluded_from_search(self):
        response = self.client.get('/search/?q=hidden')
        self.assertEqual(len(response.context['posts']), 0)

    def test_empty_query_returns_no_results(self):
        response = self.client.get('/search/')
        self.assertEqual(len(response.context['posts']), 0)

    def test_query_in_context(self):
        response = self.client.get('/search/?q=django')
        self.assertEqual(response.context['query'], 'django')
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
.venv/Scripts/python manage.py test blog.tests.SearchViewTest -v 2
```

期望：`TemplateDoesNotExist: blog/search.html`

- [ ] **Step 3: 创建 blog/templates/blog/search.html**

```html
{% extends 'blog/base.html' %}

{% block title %}搜索 "{{ query }}" - 我的博客{% endblock %}

{% block content %}
{% if query %}
<h5 class="mb-4 fw-normal text-muted">
    🔍 搜索 "<strong>{{ query }}</strong>"
    <small class="ms-2 fs-6">（共 {{ paginator.count }} 篇）</small>
</h5>
{% else %}
<h5 class="mb-4 fw-normal text-muted">🔍 搜索</h5>
{% endif %}

{% for post in posts %}
<div class="card post-card mb-3">
    {% if post.cover_image %}
    <img src="{{ post.cover_image.url }}" class="card-img-top"
         style="height:200px;object-fit:cover;" alt="{{ post.title }}">
    {% endif %}
    <div class="card-body">
        <h5 class="card-title mb-1">
            <a href="{{ post.get_absolute_url }}" class="text-decoration-none">{{ post.title }}</a>
        </h5>
        <p class="card-text text-muted small my-2">{{ post.get_summary }}</p>
        <div class="d-flex justify-content-between align-items-center">
            <div>
                {% for tag in post.tags.all %}
                <a href="{% url 'tag_posts' tag.slug %}" class="tag-badge">{{ tag.name }}</a>
                {% endfor %}
            </div>
            <small class="text-muted">{{ post.created_at|date:"Y-m-d" }}</small>
        </div>
    </div>
</div>
{% empty %}
<div class="text-center py-5">
    {% if query %}
    <p class="text-muted">没有找到与 "<strong>{{ query }}</strong>" 相关的文章。</p>
    {% else %}
    <p class="text-muted">请在上方输入关键词搜索。</p>
    {% endif %}
</div>
{% endfor %}

{% if is_paginated %}
<nav class="mt-4">
    <ul class="pagination justify-content-center">
        {% if page_obj.has_previous %}
        <li class="page-item">
            <a class="page-link" href="?q={{ query }}&page={{ page_obj.previous_page_number }}">上一页</a>
        </li>
        {% endif %}
        <li class="page-item disabled">
            <span class="page-link">{{ page_obj.number }} / {{ page_obj.paginator.num_pages }}</span>
        </li>
        {% if page_obj.has_next %}
        <li class="page-item">
            <a class="page-link" href="?q={{ query }}&page={{ page_obj.next_page_number }}">下一页</a>
        </li>
        {% endif %}
    </ul>
</nav>
{% endif %}
{% endblock %}
```

- [ ] **Step 4: 运行所有测试，确认全部通过**

```bash
.venv/Scripts/python manage.py test blog -v 2
```

期望：所有测试通过，输出类似 `Ran 30 tests in ...s  OK`

- [ ] **Step 5: Commit**

```bash
git add blog/templates/blog/search.html blog/tests.py
git commit -m "feat: add search view, complete all features"
```

---

## 完成后验证

- [ ] 启动开发服务器：

```bash
.venv/Scripts/python manage.py runserver
```

- [ ] 访问 http://127.0.0.1:8000/admin/，用 Task 3 Step 5 创建的账号登录
- [ ] 在后台创建 2-3 个标签、写一篇包含图片和富文本的文章，设为已发布
- [ ] 访问 http://127.0.0.1:8000/ 确认文章显示
- [ ] 点击标签确认筛选正常
- [ ] 搜索关键词确认搜索正常
- [ ] 点击右上角切换按钮，确认明暗模式切换并刷新后保持
